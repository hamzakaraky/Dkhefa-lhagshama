// ─────────────────────────────────────────────────────────────
//  CONSTANTS  –  shared, app-wide constant values
// ─────────────────────────────────────────────────────────────
//
// Placeholder module: no shared constants have been hoisted here yet.
// The empty `export {}` marks this file as an ES module (required under
// tsconfig `isolatedModules`) so future typed constants can be added
// without changing import call sites.

export {}
